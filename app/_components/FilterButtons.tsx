"use client";

const FilterButtons = () => {
  return (
    <div>
      <button>All</button>
      <button>Active</button>
      <button>Inactive</button>
    </div>
  );
};

export default FilterButtons;
